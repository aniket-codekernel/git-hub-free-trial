hi this is a git hub we are checking htttps work 
now we are checking a what if we change a main branch name 
lets check what will happens when a we use a ssh link without genrting ssh key
lets try with genrting ssh key